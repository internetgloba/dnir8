name: dnir8
description: DNIR8 - Browse & Download
publish_to: 'none'
version: 2.6.0+14

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  webview_flutter: 4.10.0
  shared_preferences: ^2.3.3
  path_provider: ^2.1.4
  share_plus: ^10.1.2
  open_filex: ^4.5.0
  url_launcher: ^6.3.1
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/icon/icon.png
